# connected-city
Build the project,
1.mvn clean Install
2.run the project,
3.from the browser hit below url,
https://localhost:8080/connected?origin=a&destination=b

The Connected-city can be developed using the optimal graph algorithm Breadth First Search, In that way we can see if the graph is connected
This is the basic idea of the program
